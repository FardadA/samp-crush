# Telegram Bot Token from BotFather
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# Firebase Project Credentials
# These can be found in your Firebase project settings -> Service accounts -> Generate new private key (JSON)

# Your Firebase Project ID
FIREBASE_PROJECT_ID=YOUR_FIREBASE_PROJECT_ID_HERE

# The client email from the Firebase service account JSON file
FIREBASE_CLIENT_EMAIL=your-service-account-email@your-project-id.iam.gserviceaccount.com

# The private key from the Firebase service account JSON file.
# Important: When copying this into your actual .env file or a hosting service's environment variables,
# ensure that the newlines are preserved. Some systems require you to replace actual newlines
# with the \n character. For Render, you can usually paste it as a multi-line value directly.
# Example: "-----BEGIN PRIVATE KEY-----\nxxxxxxxxxx\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_FIREBASE_PRIVATE_KEY_LINE_1\nYOUR_FIREBASE_PRIVATE_KEY_LINE_2\n...\n-----END PRIVATE KEY-----\n"

# Optional: For development, you might point to a local Firebase emulator
# FIREBASE_AUTH_EMULATOR_HOST="localhost:9099"
# FIRESTORE_EMULATOR_HOST="localhost:8080"
# FIREBASE_STORAGE_EMULATOR_HOST="localhost:9199"
